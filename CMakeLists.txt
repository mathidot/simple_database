# cmake 最低版本需求
cmake_minimum_required(VERSION 3.13)

# 工程名称
project(db)

# 设置
set(CMAKE_CXX_STANDARD 11)

# 查找当前目录下的所有源文件
# 并将名称保存到 DIR_SRCS 变量
aux_source_directory(. DIR_SRCS)


# Generate target goal
add_executable(db ${DIR_SRCS})
